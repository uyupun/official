# 環境構築

```bash
# リポジトリのクローン
$ git clone https://github.com/uyupun/official.git

# プロジェクトルートに移動
$ cd official

# 依存パッケージのインストール
$ yarn install

# huskyのセットアップ
$ yarn prepare

# 開発用サーバの起動(http://localhost:3000)
$ yarn dev
```
