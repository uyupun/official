import { sprinkles } from '../styles/sprinkles.css';

export const styles = {
  title: sprinkles({
    display: 'flex',
    alignItems: 'center',
    color: 'blue',
    fontSize: {
      mobile: 32,
      desktop: 48,
    },
  }),
};
